#include<iostream>
using namespace std;
typedef long long ll;
const ll maxn = 1e5+1;
#define nline = "\n"

ll arr[maxn];
 
ll solve(ll i, ll x)
{
}

int main()
{
	ll n, m;
	cin >> n >> m;
	for(int i = 0; i < n; i++)
		cin >> arr[i];
	if(arr[n-1] == 0)
	{
		for(int k = 1; k <= m; k++)
				solve(i-1, k)
	}
}
